apiVersion: v1
kind: Service
metadata:
  name: capstone-api
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: capstone-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
