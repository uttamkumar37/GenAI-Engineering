# placeholder values only — populate via `kubectl create secret` or a secrets manager (never commit real values)
apiVersion: v1
kind: Secret
metadata:
  name: capstone-api-secrets
  namespace: default
type: Opaque
stringData:
  OPENAI_API_KEY: "REPLACE_ME"
  ANTHROPIC_API_KEY: "REPLACE_ME"
  DATABASE_URL: "postgresql://capstone:REPLACE_ME@postgres:5432/capstone"
